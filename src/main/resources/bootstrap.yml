spring:
  application:
    name: devfest
  cloud:
    vault:
      token: root
      scheme: http
      host: "http://vault.vault.svc.cluster.local"
      port: 8200
logging:
  level:
    ROOT: INFO
    org.springframework.cloud.config: DEBUG

