spring:
  application:
    name: devfest
  cloud:
    vault:
      token: root
      scheme: http
      host: localhost
      port: 8200
logging:
  level:
    ROOT: INFO
    org.springframework.cloud.config: DEBUG

